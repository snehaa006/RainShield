# Everything needed to re-run the offline pipeline (Stages 0-3) and the tests.
-r requirements.txt

torch>=2.2            # Stage 2 training, and the numpy-vs-torch equivalence test
rasterio>=1.3         # Stage 0 raster IO
geopandas>=0.14       # Stage 0 OSM infrastructure layer
scikit-learn>=1.3     # Stage 1 scaler, Stage 2 metrics
matplotlib>=3.7       # Stage 3 dashboard figure
pytest>=8.0
httpx>=0.27           # FastAPI TestClient
