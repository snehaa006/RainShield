# CARTO basemap key. Optional — the public raster tiles also serve without one.
# Copy to .env.local (gitignored) and fill in. Vite inlines VITE_* values into
# the client bundle, so restrict this key by HTTP referrer rather than treating
# it as a secret.
VITE_BASEMAP_KEY=
